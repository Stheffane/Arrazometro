import React from "react";
import Frame from "../Frame";

import "./style.css"; 

const Body = () => {
  return (
    <section className="container">
      {/* botão para esquerda*/}
      <Frame />
      {/*botão para direita*/}
    </section>
  );
};

export default Body;
