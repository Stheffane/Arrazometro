import React from "react";
import Frame from "../Frame";

const Body = () => {
  return (
    <section className="container">
      {/* botão para esquerda*/}
      <Frame />
      {/*botão para direita*/}
    </section>
  );
};

export default Body;
